import { LibP2PManager, MessageHandler, KeyManager } from '.';

export const libp2pManager = new LibP2PManager();
export const messageHandler = new MessageHandler();
export const keyManager = new KeyManager();